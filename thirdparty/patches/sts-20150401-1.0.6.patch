--- a/external/CMakeLists.txt
+++ b/external/CMakeLists.txt
@@ -1,5 +1,6 @@
 message(STATUS "Install libraries")
 add_subdirectory(darabonba_util)
+add_subdirectory(alibabacloud_credential)
 add_subdirectory(alibabacloud_open_api)
 add_subdirectory(alibabacloud_open_api_util)
 add_subdirectory(alibabacloud_endpoint_util)
--- /dev/null
+++ b/external/alibabacloud_credential/CMakeLists.txt
@@ -0,0 +1,30 @@
+find_package(alibabacloud_credential QUIET)
+
+if(alibabacloud_credential_FOUND)
+  message(STATUS "Found alibabacloud_credential version ${alibabacloud_credential_VERSION}")
+else()
+  message(STATUS "alibabacloud_credential could not be located, Building alibabacloud_credential instead.")
+  include(FetchContent)
+
+  FetchContent_Declare(
+    _alibabacloud_credential
+    GIT_REPOSITORY https://github.com/aliyun/credentials-cpp.git
+    GIT_TAG 33fac3d
+    CMAKE_ARGS
+      -DENABLE_UNIT_TESTS:BOOL="0")
+
+  FetchContent_GetProperties(_alibabacloud_credential)
+  if(NOT _alibabacloud_credential_POPULATED)
+    FetchContent_Populate(_alibabacloud_credential)
+    message(STATUS "Added alibabacloud_credential source : ${_alibabacloud_credential_SOURCE_DIR}")
+    message(STATUS "Added alibabacloud_credential binary : ${_alibabacloud_credential_BINARY_DIR}")
+
+    set(ENABLE_UNIT_TESTS OFF)
+    set(ENABLE_UNIT_TESTS OFF CACHE BOOL "Disable unit tests" FORCE)
+    add_subdirectory(
+      ${_alibabacloud_credential_SOURCE_DIR}
+      ${_alibabacloud_credential_BINARY_DIR}
+    )
+  endif()
+  target_include_directories(alibabacloud_sts_20150401 PRIVATE "${_alibabacloud_credential_SOURCE_DIR}/include")
+endif()
--- a/external/darabonba_util/CMakeLists.txt
+++ b/external/darabonba_util/CMakeLists.txt
@@ -11,7 +11,8 @@ else()
     GIT_REPOSITORY https://github.com/alibabacloud-sdk-cpp/dara-util.git
-    GIT_TAG master
+    GIT_TAG 136c30e
     CMAKE_ARGS
-      -DENABLE_UNIT_TESTS:BOOL="0")
+      -DENABLE_UNIT_TESTS:BOOL="0"
+      -DBUILD_TESTING:BOOL="0")

   FetchContent_GetProperties(_darabonba_util)
   if(NOT _darabonba_util_POPULATED)
     FetchContent_Populate(_darabonba_util)
@@ -20,5 +21,13 @@ else()
     message(STATUS "Added darabonba_util source : ${_darabonba_util_SOURCE_DIR}")
     message(STATUS "Added darabonba_util binary : ${_darabonba_util_BINARY_DIR}")
+
+    # Force Boost static linking and disable tests for subproject
+    set(BUILD_TESTING OFF CACHE BOOL "Disable tests" FORCE)
+    set(ENABLE_UNIT_TESTS OFF)
+    set(ENABLE_UNIT_TESTS OFF CACHE BOOL "Disable unit tests" FORCE)
+    set(Boost_USE_STATIC_LIBS ON CACHE BOOL "Use static Boost libraries" FORCE)
+    set(Boost_USE_STATIC_RUNTIME ON CACHE BOOL "Use static Boost runtime" FORCE)
+    set(BUILD_SHARED_LIBS OFF CACHE BOOL "Build static libraries" FORCE)
+
     add_subdirectory(
       ${_darabonba_util_SOURCE_DIR}
       ${_darabonba_util_BINARY_DIR}
--- a/external/darabonba_core/CMakeLists.txt
+++ b/external/darabonba_core/CMakeLists.txt
@@ -11,7 +11,8 @@ else()
     GIT_REPOSITORY https://github.com/aliyun/tea-cpp.git
-    GIT_TAG master
+    GIT_TAG 3519821
     CMAKE_ARGS
-      -DENABLE_UNIT_TESTS:BOOL="0")
+      -DENABLE_UNIT_TESTS:BOOL="0"
+      -DBUILD_TESTING:BOOL="0")

   FetchContent_GetProperties(_darabonba_core)
   if(NOT _darabonba_core_POPULATED)
     FetchContent_Populate(_darabonba_core)
@@ -20,5 +21,13 @@ else()
     message(STATUS "Added darabonba_core source : ${_darabonba_core_SOURCE_DIR}")
     message(STATUS "Added darabonba_core binary : ${_darabonba_core_BINARY_DIR}")
+
+    # Force Boost static linking and disable tests for subproject
+    set(BUILD_TESTING OFF CACHE BOOL "Disable tests" FORCE)
+    set(ENABLE_UNIT_TESTS OFF)
+    set(ENABLE_UNIT_TESTS OFF CACHE BOOL "Disable unit tests" FORCE)
+    set(Boost_USE_STATIC_LIBS ON CACHE BOOL "Use static Boost libraries" FORCE)
+    set(Boost_USE_STATIC_RUNTIME ON CACHE BOOL "Use static Boost runtime" FORCE)
+    set(BUILD_SHARED_LIBS OFF CACHE BOOL "Build static libraries" FORCE)
+
     add_subdirectory(
       ${_darabonba_core_SOURCE_DIR}
       ${_darabonba_core_BINARY_DIR}
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -23,7 +23,7 @@ message(STATUS "Build type: ${CMAKE_BUILD_TYPE}")

 # <<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<< Target set up >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> #
-add_library(${PROJECT_NAME} SHARED "")
+add_library(${PROJECT_NAME} STATIC "")
 set(headers
         include/alibabacloud/sts_20150401.hpp)
 target_sources(${PROJECT_NAME}
